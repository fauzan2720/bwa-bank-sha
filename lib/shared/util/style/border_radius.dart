import 'package:flutter/material.dart';

BorderRadiusGeometry cardRadius = BorderRadius.circular(cardRadiusSize);
BorderRadiusGeometry inputRadius = BorderRadius.circular(inputRadiusSize);
BorderRadiusGeometry buttonRadius = BorderRadius.circular(buttonRadiusSize);

double cardRadiusSize = 20.0;
double inputRadiusSize = 14.0;
double buttonRadiusSize = 56.0;
